# فضای توسعه اختصاصی اودو — نسخه اولیه

## مشخصات مبنا

- مخزن توسعه: `hamlatai1351-ai/odoo`
- مخزن بالادستی: `odoo/odoo`
- شاخه اصلی نسخه متن‌باز: `19.0`
- شاخه اختصاصی توسعه: `development/community-customization`
- Commit مبنای ایجاد شاخه توسعه: `82f4b92eaf3f2014eb1667e4845e80c377dbfb4f`
- تاریخ ایجاد فضای توسعه: `2026-09-18`
- وضعیت: محیط مخزن آماده توسعه است؛ نصب، اجرای برنامه و آزمون‌ها هنوز تأیید نشده‌اند.

## هدف و مرز تغییرات

این شاخه محل توسعه مستقل روی Odoo Community 19 است. کد بالادستی، تاریخچه Git و مجوزهای موجود حفظ می‌شوند. تا زمانی که برای یک تغییر دلیل مشخص وجود ندارد، کد هسته و ماژول‌های استاندارد تغییر نکنند؛ افزونه‌های مستقل در `custom_addons/` قرار گیرند. این سند صرفاً نقطه شروع و روش کار است و دامنه محصول، امکانات سفارشی و معیارهای پذیرش هنوز تعیین نشده‌اند.

## شیوه توسعه

1. مبنا را در شاخه `19.0` از بالادستی نگه دارید. تغییرات سفارشی را روی شاخه توسعه یا شاخه‌های کوتاه‌عمر منشعب از آن انجام دهید.
2. برای هر قابلیت، وابستگی‌های ماژول، مدل داده، تغییرات امنیتی، سناریوی آزمون و اثر ارتقا را در Pull Request تشریح کنید.
3. ابتدا در پایگاه داده آزمایشی توسعه و آزمون انجام دهید؛ هیچ تغییر آزمایشی را مستقیماً روی اطلاعات عملیاتی اجرا نکنید.
4. همگام‌سازی بالادستی را با بررسی Diff و آزمون انجام دهید؛ از جایگزینی اجباری تاریخچه یا Merge خودکار تغییرات بررسی‌نشده خودداری کنید.
5. تغییرات را ابتدا با Pull Request به شاخه توسعه بیاورید؛ تغییر شاخه `19.0` تنها با تصمیم صریح جداگانه انجام شود.
6. برای هر افزونه ثالث مجوز، سازگاری با نسخه ۱۹ و منبع آن را مستقل بررسی کنید. مجوز فایل `LICENSE` مخزن اصلی و اطلاعیه‌های مجوز اجزای ثالث باید محفوظ بمانند.

## کلون در سیستم توسعه‌دهنده

دستورهای زیر فقط راهنمای اجرا در رایانه یا محیطی هستند که Git و دسترسی شبکه دارد؛ اجرای آنها در این جلسه انجام نشده است.

```powershell
git clone --branch development/community-customization https://github.com/hamlatai1351-ai/odoo.git
cd odoo
git remote add upstream https://github.com/odoo/odoo.git
git remote -v
```

اگر Remote با نام `upstream` از قبل وجود دارد، به‌جای اجرای مجدد دستور افزودن، نشانی آن را بررسی کنید. توجه: مخزن اودو بزرگ است؛ پیش از کلون، ظرفیت دیسک و اتصال را بررسی کنید.

## توسعه افزونه‌ها

- مسیر افزونه‌های اختصاصی: `custom_addons/`
- این پوشه تا زمان ساخت نخستین ماژول، تنها راهنما دارد؛ ماژول اجرایی هنوز ایجاد نشده است.
- هنگام راه‌اندازی محلی، پوشه اختصاصی باید مطابق مستندات نسخه ۱۹ در `addons_path` قرار گیرد. نصب پیش‌نیازها، پیکربندی PostgreSQL، فایل پیکربندی محلی و تست اجرا هنوز انجام نشده است.
- رمزها، کلیدها، فایل‌های محیطی حاوی اسرار، پشتیبان پایگاه داده و داده واقعی مشتری را Commit نکنید.

## وضعیت تحویل

- [x] تأیید Fork و سطح دسترسی نوشتن
- [x] ایجاد شاخه توسعه از Commit مشخص نسخه ۱۹
- [x] ثبت راهنمای اولیه توسعه و مسیر افزونه‌های مستقل
- [ ] تعیین دامنه نخستین قابلیت و معیارهای پذیرش
- [ ] آماده‌سازی محیط اجرایی و پایگاه داده آزمایشی
- [ ] ساخت و آزمون نخستین افزونه اختصاصی
- [ ] تنظیم و اجرای آزمون‌های خودکار

هیچ تغییر کاربردی در هسته اودو یا امکانات نرم‌افزار در این مرحله پیاده‌سازی نشده است.
